test="cos"
xxx="cos"

var1=a
var2=a
var3=b
var4=b
var5=e
var6=d

begin
        if(var1 = var2)
                msg(1st if begin)

                if(var3 = var4)
                        msg(2st if begin)

                        if(var5 != var6)
                                msg(3rd)
                        endif

                        msg(2st if end)
                endif

                msg(1st if end)
        endif

        msg()
        msg(message 1)
        msg(next line of message)
        setitem(item1, description 1)
        setitem(item2,description 2)
        setitem(item3, Fdsafsdf)
        setitem(item4,fdsafd)
        readsel(select,)

        setitem(XXXX,)
        setitem(YYYY,)
        readsel(sel,)

        keycreate(header, ./test.h, SELECTION, select)
        readint(var, )
        keycreate(header, ./test.h, MOJA_ZMIENNA, var)
        readuint(2, )
        readstring(string, )
        keycreate(makefile, ./test.mk, MY_STRING, string)
        keycreate(header, ./test.h, MY_STRING, string)
        test=dupa
        print(To jest zmienna @test i cos tam robi)
        keysave(makefile, ./test.mk, MK_TEST_5, xxx)
        xxx=0
        keysave(header, ./test.h, H_TEST_4, xxx)

        item=?
        keyread(makefile, ./test.mk, ITEM, item)
        keysave(makefile, ./test.mk, READ, item)

        keyread(header, ./test.h, ITEM, item)
        keysave(header, ./test.h, READ, item)
end

# comment

\begin{}
        \keyread{makefile}{xxx.mk}{FSCONFIG__LFS_ENABLE}{0}
        \msg{message 2}
        \msg{next line of message}
        \item{item1}{description 1}
        \item{item2}{description 2}
        \item{item3}{}
        \item{item4}{fdsafd}
        \readsel{0}{}
        \readint{1}{fdsfa}
        \readuint{2}{fda}
        \readstring{3}{fda}
        \keysave{makefile}{xxx.mk}{FSCONFIG__LFS_ENABLE}{0}
        \keycreate{makefile|header}{file}{2}{1}
        \keydelete{makefile|header}{file}{3}
\end{ed}
