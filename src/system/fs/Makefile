# Makefile for GNU make
CSRC_CORE   += fs/fsctrl.c
CSRC_CORE   += fs/pipe.c
CSRC_CORE   += ../../$(GEN_FS_DIR)/fs_registration.c
CSRC_CORE   += fs/vfs.c
HDRLOC_CORE += fs

-include $(GEN_FS_DIR)/Makefile.in
